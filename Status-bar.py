from tkinter import * 

root = Tk()

status = Label(root , text = "Prpearing to do nothing ...."
              , bd=1 , relief = SUNKEN , anchor = W )

status.pack(side = BOTTOM , fill = X)

root.mainloop()
